{
    "build": {
      "builder": "NIXPACKS",
      "buildCommand": "",
      "watchPatterns": ["backend/**"],
      "rootDirectory": "backend"
    },
    "deploy": {
      "startCommand": "uvicorn app:app --host 0.0.0.0 --port 8000"
    }
  }
  